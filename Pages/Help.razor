<MudDialog DisableSidePadding="true" Style="max-height: 100%; overflow-y: scroll;">
	<DialogContent>
		<MudContainer>
			<MudText Typo="Typo.h5" Class="px-4">Five Letter Word Guess</MudText>
			<MudText Typo="Typo.h6" Class="px-4">A shameless clone of the ever-popular W*****e.</MudText>
			<MudText Typo="Typo.h6" Class="px-4">How to Play</MudText>
			<MudText Typo="Typo.body2" Class="px-4">Guess the Five Letter Word in six tries.</MudText>
			<MudText Typo="Typo.body2" Class="mt-2 pa-4">Each guess must be a valid five-letter word. Hit the enter button to submit.</MudText>
			<MudText Typo="Typo.body2" Class="mt-2 pa-4">In contrast to W*****e, the words in this game will never have repeating letters. (i.e. SPOOL is not in our word list).</MudText>
			<MudText Typo="Typo.body2" Class="my-2 pa-4">After each guess, the color of the tiles will change to show how close your guess was to the word.</MudText>
			<MudDivider />
			<MudText Typo="Typo.h6" Class="px-4">Examples</MudText>
			<MudImage ObjectPosition="ObjectPosition.Left" Src="img/RightCharRightPosition.png"></MudImage>
			<MudText Typo="Typo.body2">The letter <strong>E</strong> is in the word and in the correct spot.</MudText>
			<MudImage ObjectPosition="ObjectPosition.Left" Src="img/RightCharWrongPosition.png"></MudImage>
			<MudText Typo="Typo.body2">The letter <strong>E</strong> is in the word but in the wrong spot.</MudText>
			<MudImage ObjectPosition="ObjectPosition.Left" Src="img/WrongChars.png"></MudImage>
			<MudText Typo="Typo.body2">None of the letters are in the word.</MudText>
			<MudDivider />
			<MudText Typo="Typo.h6" Class="my-3">Settings</MudText>
			<MudText Typo="Typo.body2" Class="d-flex">Click on the <MudIcon Icon="@Icons.Material.Filled.Settings" Class="mx-1" /> button in the nav bar to modify settings.</MudText>
		</MudContainer>
	</DialogContent>
	<DialogActions>
		<MudButton Variant="Variant.Outlined" @onclick="Done">OK</MudButton>
	</DialogActions>
</MudDialog>

@code
{
	[CascadingParameter]
	MudDialogInstance? MudDialog { get; set; }

	void Done()
	{
		MudDialog!.Close(DialogResult.Ok(true));
	}
}
